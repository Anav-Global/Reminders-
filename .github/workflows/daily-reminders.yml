name: Daily Task Reminders

on:
  schedule:
    # 13:30 UTC = 7:00 PM IST (India is UTC+5:30). GitHub Actions cron
    # is always in UTC — there is no timezone field.
    - cron: "30 13 * * *"
  workflow_dispatch: {}
    # Lets you manually trigger this from the GitHub Actions tab for
    # testing, instead of waiting for the scheduled time.

jobs:
  send-reminders:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Send reminder emails
        env:
          FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          SYSTEM_USER_EMAIL: ${{ secrets.SYSTEM_USER_EMAIL }}
          SYSTEM_USER_PASSWORD: ${{ secrets.SYSTEM_USER_PASSWORD }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
        run: node send-reminders.js
